{% extends 'base.html.twig' %}

{% block body_id 'add_notification_config' %}

{% block main %}
    <h1> Add Notification Config </h1>

    {{ form_start(form) }}
        {{ form_row(form.header) }}
        {{ form_row(form.apikey) }}
        {{ form_row(form.url) }}
        {{ form_row(form.platform) }}
        {#<button type="submit" class="btn btn-primary">#}
            {#<i class="fa fa-save" aria-hidden="true"></i> Add Notification Config#}
        {#</button>#}
    {{ form_end(form) }}
{% endblock %}

